import { DefaultTheme } from "styled-components";

const appTheme: DefaultTheme = {
  borderRadius: "5px",

  colors: {
    main: "#fdfdfd",
  },

  sizes: {
    sm: "1.4rem",
  },
};

export { appTheme };
